{
  "mcpServers": {
    "plex": {
      "command": "npx",
      "args": ["-y", "-p", "@sshanzel/plex@0.10.0", "plex-mcp"],
      "env": { "npm_config_registry": "https://registry.npmjs.org/" }
    }
  }
}
